import React from 'react'

const MessageBubble = ({message,onReact,deleteMessage,theme,currentUser}) => {
    console.log("This is my message",message);
  return (
    <div>MessageBubble</div>
  )
}

export default MessageBubble